using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OldCar : Object
{
    // Start is called before the first frame update
    void Start()
    {
        name = "오래된 차";
        explanation = "너무 오래되서 작동하지 않는다";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
